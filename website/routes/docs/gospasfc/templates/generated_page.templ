// Code generated by GoSPA; DO NOT EDIT.

package templates

import "github.com/aydenstechdungeon/gospa/state"
import "github.com/aydenstechdungeon/gospa/website/components"

type PageProps struct {
}

templ Page() {


	if r := state.FromContext(ctx); r != nil {
		pMap := map[string]interface{}{
			
		}
		r.Register("routesdocsgospasfctemplatespage", pMap, nil)
	}
	<div data-gospa-island="routesdocsgospasfctemplatespage" class="gospa-985f">
		<div class="space-y-12 gospa-985f">
		<header class="gospa-985f">
			<div class="flex items-center gap-2 mb-4 gospa-985f">
				<span class="px-2 py-1 rounded bg-[var(--accent-primary)]/10 text-[var(--accent-primary)] text-xs font-bold uppercase tracking-wider gospa-985f">Features</span>
				<span class="text-[var(--text-muted)] text-sm gospa-985f">/</span>
				<span class="text-[var(--text-muted)] text-sm font-medium gospa-985f">Gospa SFC</span>
			</div>
			<h1 class="text-4xl font-bold tracking-tight mb-4 text-transparent bg-clip-text bg-gradient-to-r from-[var(--accent-primary)] to-[var(--accent-secondary)] gospa-985f">Template Syntax</h1>
			<p class="text-xl text-[var(--text-secondary)] leading-relaxed gospa-985f">
				Seamless Go templates with reactive interactive features.
			</p>
		</header>

		<section class="space-y-8 gospa-985f">
			<div class="gospa-985f">
				<h2 class="text-2xl font-bold mb-4 border-b border-[var(--border)] pb-2 italic mono gospa-985f">Standard Go in Templates</h2>
				<p class="text-[var(--text-secondary)] mb-6 gospa-985f">
					GoSPA SFC templates use the familiar Templ syntax. Any Go variable or function available in your <code class="gospa-985f">&lt;script lang=&gt;</code> block can be used directly in the template.
				</p>
				@components.CodeBlock(`<template>
  <div class="user-profile">
    <h2>User: {user.Name}</h2>
    <p>Age: {fmt.Sprintf("%d", user.Age)}</p>
  </div>
</template>`, "svelte", "Profile.gospa")
			</div>

			<div class="gospa-985f">
				<h2 class="text-2xl font-bold mb-4 border-b border-[var(--border)] pb-2 italic mono gospa-985f">Control Flow</h2>
				<p class="text-[var(--text-secondary)] mb-6 gospa-985f">
					Directly include Go control flow logic within your templates using Templ's <code class="gospa-985f">if</code>, <code class="gospa-985f">else</code>, and <code class="gospa-985f">for</code> directives.
				</p>
				@components.CodeBlock(`<template>
  {if count > 10}
    <p>Danger: Count too high!</p>
  {else}
    <p>Safe level.</p>
  {/if}

  <ul>
    {for _, item := range items}
      <li>{item.Name}</li>
    {/for}
  </ul>
</template>`, "svelte", "Control.gospa")
			</div>

			<div class="gospa-985f">
				<h2 class="text-2xl font-bold mb-4 border-b border-[var(--border)] pb-2 italic mono gospa-985f">Event Binding</h2>
				<p class="text-[var(--text-secondary)] mb-6 gospa-985f">
					Bind user interactions to Go functions or TypeScript actions using the <code class="gospa-985f">on:&lt;event&gt;</code> syntax.
				</p>
				<table class="w-full text-sm text-left text-[var(--text-secondary)] mb-6 gospa-985f">
					<thead class="text-xs uppercase bg-[var(--bg-secondary)] text-[var(--text-primary)] gospa-985f">
						<tr class="gospa-985f"><th class="px-4 py-2 gospa-985f">Event</th><th class="px-4 py-2 gospa-985f">Binding Example</th><th class="px-4 py-2 gospa-985f">Effect</th></tr>
					</thead>
					<tbody class="gospa-985f">
						<tr class="border-b border-[var(--border)] gospa-985f"><td class="px-4 py-2 gospa-985f">Click</td><td class="px-4 py-2 font-mono gospa-985f">on:click=&lcub;handleClick&rcub;</td><td class="px-4 py-2 gospa-985f">Triggered on mouse click.</td></tr>
						<tr class="border-b border-[var(--border)] gospa-985f"><td class="px-4 py-2 gospa-985f">Input</td><td class="px-4 py-2 font-mono gospa-985f">on:input=&lcub;updateValue&rcub;</td><td class="px-4 py-2 gospa-985f">Triggered on value change.</td></tr>
						<tr class="border-b border-[var(--border)] gospa-985f"><td class="px-4 py-2 gospa-985f">Submit</td><td class="px-4 py-2 font-mono gospa-985f">on:submit=&lcub;handleForm&rcub;</td><td class="px-4 py-2 gospa-985f">Triggered on form submission.</td></tr>
					</tbody>
				</table>
			</div>

			<div class="flex justify-between items-center pt-8 gospa-985f">
				<a href="/docs/gospasfc/reactivity" class="flex flex-col items-start gap-1 p-4 rounded-xl border border-[var(--border)] hover:bg-[var(--bg-secondary)] transition-all gospa-985f">
					<span class="text-xs text-[var(--text-muted)] font-bold uppercase tracking-widest gospa-985f">Previous</span>
					<span class="font-bold text-[var(--accent-primary)] gospa-985f">Runes & Reactivity</span>
				</a>
				<a href="/docs/gospasfc/styles" class="flex flex-col items-end gap-1 p-4 rounded-xl border border-[var(--border)] hover:bg-[var(--bg-secondary)] transition-all gospa-985f">
					<span class="text-xs text-[var(--text-muted)] font-bold uppercase tracking-widest gospa-985f">Next</span>
					<span class="font-bold text-[var(--accent-primary)] gospa-985f">Scoped CSS</span>
				</a>
			</div>
		</section>
	</div>
	</div>
}
