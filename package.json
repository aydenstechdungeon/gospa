{
    "name": "gospa",
    "version": "0.1.7",
    "private": true,
    "scripts": {
        "check": "cd client && bun check",
        "format": "cd client && bun format",
        "lint": "cd client && bun lint"
    }
}